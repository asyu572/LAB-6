---
- name: Deploy Artifact to Localhost
  hosts: localhost
  tasks:
  - name: Copy the artifact to the target location
    become: true
    become_user: aps
    become_method: su
    copy:
          src: "/var/lib/jenkins/workspace/ff/target/my-app-1.0-SNAPSHOT.jar"
          dest: "/home/aps/t.jar"
